#include<stdio.h>
#include<string.h>
int main()
{
    char str1[10],count=0,i;
    printf("enter a string1\n");
    scanf("%s",str1);
    int len1=strlen(str1);
    char str2[10];
    printf("enter a string2\n");
    scanf("%s",str2);
    int len2=strlen(str2);
    for(i=0;i<=len1-1;i++)
    {
      if (str1[i]==str2[i])
    {
        count++;
    }
    }
    
    if(count==len1)
    {
        printf("same\n");
    }
    else
    {
        printf("not same\n");
    }
}
